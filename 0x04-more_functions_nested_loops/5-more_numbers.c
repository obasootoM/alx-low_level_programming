#include "main.h"
/**
 * more_numbers - print the numbers since 0 - 9.
 * Return: return the numbers 0 up to 9.
 */
void more_numbers(void)
{
int j;
int i;
for (i = 0; c < 10; i++)
{
for (j = 0; j <= 14; j++)
{
if (j > 9)
{
_putchar((y / 10) + '0');
}
_putchar((y % 10) + '0');
}
_putchar('\n');
}
}
