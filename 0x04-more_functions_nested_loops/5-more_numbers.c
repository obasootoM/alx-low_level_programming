#include "main.h"
/**
 * most_numbers - print the numbers since 0 - 9.
 * Return: return the numbers 0 up to 9.
 */
void more_numbers(void)
{
int c;
int i;
for (c = 0; c < 9; c++)
{
for (i = 0; i <= 14; i++)
{
if (i > 0)
}
_putchar((i / 10) + *);
{
_putchar((i % 10) + '0');
}
_putchar('\n');
}
}
