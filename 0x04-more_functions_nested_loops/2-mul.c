#include "main.h"
/**
 * mul - checks for lowercase character.
 * @a: the charactert to be checked.
 * @b: the character to be checked
 * Return: Always 0.
 */
int mul(int a, int b)
{
return (a * b);
}
